/**
 * 항목별 지시문.
 * 여기 문구가 곧 자료의 품질이다. 선생님이 고칠 수 있도록 나중에 편집 UI를 붙인다.
 */

export const SYSTEM = `당신은 대한민국 고등학교 영어 교사를 돕는 학습자료 제작 전문가입니다.
수능·모의고사 영어 지문을 분석해 수업에 바로 쓸 수 있는 부교재를 만듭니다.

반드시 지키세요.
- 원문 영어 문장은 절대 수정·요약·생략하지 않습니다. 철자와 구두점까지 그대로 옮깁니다.
- 설명과 해석은 모두 한국어로 씁니다. 번역투를 피하고 교사가 그대로 읽어줄 수 있는 문장으로 씁니다.
- 지문에 없는 사실을 지어내지 않습니다. 근거는 항상 지문 안에서 찾습니다.
- 분량 지시(몇 문장, 몇 자)를 지킵니다. A4 한 장에 인쇄되므로 넘치면 잘립니다.
- 요청한 JSON 형식만 출력합니다. 설명이나 인사말을 덧붙이지 않습니다.`

interface Ctx {
  passage: string
  sentences: string[]
  /** 앞서 만든 단어 리스트 — 뒤 항목에서 용어를 맞추는 데 쓴다 */
  words?: string[]
}

const passageBlock = (c: Ctx) =>
  `[지문]\n${c.passage}\n`

const sentenceBlock = (c: Ctx) =>
  `[문장 목록] (번호 순서를 그대로 지킬 것)\n${c.sentences.map((s, i) => `${i + 1}. ${s}`).join('\n')}\n`

export const PROMPTS: Record<string, (c: Ctx) => string> = {
  translation: (c) => `${passageBlock(c)}
${sentenceBlock(c)}
위 지문의 '지문 해석' 자료를 만드세요.

1. titleEn / titleKo — 지문 내용을 압축한 제목.
2. sentences — [문장 목록]의 ${c.sentences.length}개 문장을 순서대로 모두 포함합니다.
   en 은 원문 그대로, ko 는 자연스러운 우리말 해석입니다.
   해석은 좁은 단에 인쇄되므로 한 문장당 60자 안팎으로 간결하게 씁니다.
3. words — 지문에서 학생이 어려워할 단어 6~9개. 본문 등장 순서대로.`,

  key_guide: (c) => `${passageBlock(c)}
위 지문의 '핵심 가이드' 한 장을 만드세요. 교사가 수업 전 5분 만에 지문을 장악할 수 있어야 합니다.

- structure 는 반드시 '도입', '전개', '결론' 세 개입니다. points 는 각각 2~3개.
- examLogic / syntaxPoint / vocabPrep / contentPoint 는 수능 출제 관점에서 씁니다.
- syntaxPoint 는 원문 문장을 큰따옴표로 인용한 뒤 문법 구조를 짚습니다.
- insight.examinerView 는 "(주제 / 빈칸)" 처럼 예상 유형을 앞에 밝히고 시작합니다.`,

  sentence_read: (c) => `${passageBlock(c)}
${sentenceBlock(c)}
위 지문의 '한 문장 읽기' 자료를 만드세요. 학생이 문장을 하나씩 소화하도록 돕는 자료입니다.

items 는 [문장 목록]의 ${c.sentences.length}개 문장에 1:1로 대응하며 순서가 같아야 합니다.
- ko: 그 문장의 해석
- summary: 반드시 [앞 개념] → [뒤 개념] 형식. 예) [읽기와 연구] → [읽기가 반드시 혼자 하는 활동은 아님]
- example: 일상에서 겪을 법한 쉬운 비유. 학생에게 말하듯 "~예요", "~거예요" 로 끝냅니다.`,

  close_reading: (c) => `${passageBlock(c)}
위 지문의 '정밀 판독' 자료를 만드세요. 학생이 지문을 대충 읽고 넘어가지 못하게 하는 확인 질문입니다.

items 는 정확히 6개이며, category 는 아래 순서를 그대로 따릅니다.
1) 정보 확인 — 지문에 명시된 사실을 찾게 하는 질문
2) 핵심 원리 — 현상이 작동하는 원리를 묻는 질문
3) 함정 주의 — 학생이 반대로 이해하기 쉬운 지점을 "~인가?" 로 묻고, 답을 "아니다/그렇다"로 시작
4) 인과 관계 — 원인과 결과의 연결을 묻는 질문
5) 지칭 추론 — 대명사나 지시어가 가리키는 대상을 묻는 질문. 해당 영어 표현을 질문에 인용
6) 요지 파악 — 필자의 핵심 주장을 묻는 질문

answer 에는 근거가 되는 영어 표현을 괄호 안에 함께 적습니다.`,

  vocab_list: (c) => `${passageBlock(c)}
위 지문의 '전체 단어 리스트'를 만드세요.

- 지문에 실제로 나온 단어만 고릅니다. 본문 등장 순서대로 정렬합니다.
- 고등학생 기준으로 짚고 넘어갈 단어 10~14개. 관사·전치사 같은 기능어는 제외합니다.
- word 는 반드시 원형으로 적습니다. (improves → improve, discussing → discuss)
- 지문에서 쓰인 의미를 먼저 적습니다. 사전의 첫 번째 뜻이 아니라 문맥상의 뜻입니다.
- syn / ant 는 고등학생이 알 만한 수준의 단어로 고릅니다.`,
}
